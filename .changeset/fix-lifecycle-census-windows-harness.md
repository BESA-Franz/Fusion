---
"@runfusion/fusion": patch
---

summary: Make the lifecycle-column census and its regression harness portable on Windows.
category: fix
dev: Use argument-based Git discovery rooted explicitly at the repository, convert file URLs correctly, launch Node through process.execPath, and inject GitHub claim fixtures without touching the network.
