---
"@runfusion/fusion": patch
---

summary: Make native worktree removal recovery deterministically testable.
category: fix
dev: Allow the configured Git runner to drive the non-empty fallback seam while preserving the default native runner and validation errors.
