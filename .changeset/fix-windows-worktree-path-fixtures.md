---
"@runfusion/fusion": patch
---

summary: Keep worktree path and pinning tests portable across operating systems.
category: fix
dev: Uses platform-native absolute path fixtures instead of hard-coded POSIX outputs on Windows.
