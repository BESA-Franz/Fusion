---
"@runfusion/fusion": patch
---

summary: Keep ghost-reference real-Git coverage portable on Windows.
category: fix
dev: Invoke fixture Git operations with argument arrays so commit messages and worktree paths retain their exact values on every platform.
