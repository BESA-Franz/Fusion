---
"@runfusion/fusion": patch
---

summary: Make worktree-pool lifecycle regressions portable across host path formats.
category: fix
dev: Resolve registered paths and join filesystem paths with the host path API in test fixtures.
